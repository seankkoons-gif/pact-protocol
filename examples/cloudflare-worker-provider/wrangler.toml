name = "pact-cloudflare-worker-provider"
main = "src/worker.ts"
compatibility_date = "2024-01-01"

[env.production]
name = "pact-provider-prod"

# Example: Uncomment to use KV for storing negotiation state
# [[kv_namespaces]]
# binding = "NEGOTIATIONS"
# id = "your-kv-namespace-id"

# Example: Uncomment to use secrets (set via `wrangler secret put PROVIDER_SECRET`)
# # Secrets are accessed via env.PROVIDER_SECRET in worker code
