/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enable serverless functions (Vercel default)
  // Edge runtime optional: uncomment to use Edge runtime
  // experimental: {
  //   runtime: 'edge',
  // },
};

module.exports = nextConfig;
