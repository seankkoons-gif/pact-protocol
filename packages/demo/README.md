# @pact/demo

Demos for Pact v1.

From repo root:

- Terminal A: `pnpm provider:serve`
- Terminal B: `pnpm demo:v1:happy` (for v1 transcripts) or `pnpm demo:happy` (for v4 transcripts)

Other demos:
- `pnpm demo:price-too-high`
- `pnpm demo:timeout`
- `pnpm demo:bad-reveal`
